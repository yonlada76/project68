<!--หน้าบุคคลภายนอกหลังสมัครเป็นสมาชิก-->
<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <title>หน้าของฉัน (บุคคลภายนอก)</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="/public/style.css" />
  <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    .section-title{font-weight:700}
    .info-label{color:#6c757d;font-size:.92rem}
    .qr-box{border:1px dashed #cfe6d7;border-radius:14px;padding:18px;background:#f7fbf9}
    .card-action{border:0;border-radius:16px;box-shadow:0 6px 20px rgba(0,0,0,.06)}
    .card-action .icon{font-size:28px}
    .badge-type{background:#eaf8f0;color:#198754;border-radius:999px;font-weight:600}
  </style>
</head>
<body class="bg-light">

   <!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-success shadow-sm">
  <div class="container">
    <a class="navbar-brand fw-semibold" href="/">🏋️ ระบบยืม-คืน & ฟิตเนส</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div id="nav" class="collapse navbar-collapse">
      <ul class="navbar-nav ms-auto align-items-center gap-2">
        <li class="nav-item">
          <a class="nav-link active" href="/external-home">หน้าแรก</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="/history">ประวัติการเข้าใช้ฟิตเนส</a>
        </li>
        <li class="nav-item">
  <form action="/logout" method="post" class="d-inline">
    <button 
      type="submit" 
      class="nav-link btn btn-link text-light px-2 py-2" 
      style="text-decoration: none;">
      ออกจากระบบ
    </button>
  </form>
</li>
      </ul>
    </div>
  </div>
</nav>

  <!-- Content -->
  <main class="container my-4 my-md-5">
    <div class="row g-4 align-items-stretch">
      <!-- Left: info + actions -->
      <div class="col-12 col-lg-8">
        <div class="card border-0 shadow p-4 p-md-5">
          <div class="d-flex align-items-center justify-content-between flex-wrap gap-2">
            <div>
              <span class="badge badge-type px-3 py-2">👤 บุคคลภายนอก</span>
              <h2 class="section-title mt-3 mb-1">สวัสดี <%= user?.full_name || 'ผู้ใช้งาน' %></h2>
              <p class="text-muted mb-0">
                คุณสามารถเข้าใช้ฟิตเนสได้โดยแสดง QR Code ให้เจ้าหน้าที่สแกน (ค่าบริการ <strong>30 บาท</strong>)
              </p>
            </div>
          </div>

          <!-- Info blocks -->
          <div class="row g-3 mt-4">
            <div class="col-12 col-md-6">
              <div class="p-3 border rounded-3 bg-white h-100">
                <div class="info-label mb-1">อีเมล</div>
                <div class="fw-semibold"><%= user?.email || '-' %></div>
              </div>
            </div>
            <div class="col-12 col-md-6">
              <div class="p-3 border rounded-3 bg-white h-100">
                <div class="info-label mb-1">เลขบัตรประชาชน</div>
                <div class="fw-semibold"><%= user?.external_id || '-' %></div>
              </div>
            </div>
          </div>

          <!-- Actions (เฉพาะประวัติ) -->
          <div class="row g-3 mt-4">
            <div class="col-12">
              <a class="card card-action h-100 p-3 text-decoration-none" href="/history">
                <div class="icon">🗂️</div>
                <div class="fw-bold mt-2">ประวัติการเข้าใช้ฟิตเนส</div>
                <div class="text-muted small">ดูรายการที่เจ้าหน้าที่บันทึกให้</div>
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- Right: QR -->
      <div class="col-12 col-lg-4">
        <div class="card border-0 shadow p-4 h-100 d-flex">
          <h5 class="fw-bold mb-3">QR Code ของฉัน</h5>
          <div class="qr-box text-center flex-grow-1 d-flex flex-column align-items-center justify-content-center">
            <% if (user?.qr_code_value) { %>
              <img class="img-fluid mb-2" style="max-width:220px"
                   src="/qrcode/<%= encodeURIComponent(user.qr_code_value) %>"
                   alt="QR Code">
              <div class="fw-semibold mt-1"><%= user?.external_id %></div>
            <% } else { %>
              <div class="text-muted">ยังไม่มี QR Code</div>
            <% } %>
          </div>
          <div class="text-muted small mt-2 text-center">
            แสดง QR นี้ให้เจ้าหน้าที่สแกนเพื่อเข้าใช้ฟิตเนส (ค่าบริการ 30 บาท/ครั้ง)
          </div>
        </div>
      </div>
    </div>
  </main>

  <footer class="py-4 text-center text-muted small">
    © <script>document.write(new Date().getFullYear())</script> Sports & Fitness Center
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
