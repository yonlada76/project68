<!--หน้ากรอกข้อมูลบุคคลภายนอก-->
<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <title>สมัครสมาชิกบุคคลภายนอก</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <!-- Global styles -->
  <link rel="stylesheet" href="/public/style.css" />
  <!-- ฟอนต์ -->
  <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    body{background:#f8f9fa;font-family:"Prompt",system-ui,-apple-system,"Segoe UI",Roboto,"Helvetica Neue",Arial}
    .page-wrap{min-height:100dvh;display:flex;align-items:center;justify-content:center;padding:24px}
    .card{border:0;border-radius:18px;box-shadow:0 12px 30px rgba(0,0,0,.08)}
    .form-label{font-weight:600}
    .hint{font-size:.9rem;color:#6c757d}
    .header-badge{display:inline-flex;align-items:center;gap:.5rem;background:#eaf8f0;color:#198754;border-radius:999px;padding:.35rem .75rem;font-weight:600}
    .btn-lg{border-radius:12px;padding:.9rem 1rem}
    .req::after{content:" *";color:#dc3545}
  </style>
</head>
<body>
  <div class="page-wrap">
    <div class="container" style="max-width:640px">
      <div class="card overflow-hidden">
          <div class="p-4 p-md-5">
            <div class="mb-3">
              <span class="header-badge">👤 สมัครสมาชิก (บุคคลภายนอก)</span>
            </div>
            <h2 class="fw-bold mb-2">กรอกข้อมูลเพื่อสมัคร</h2>
            <p class="text-muted mb-4">ใช้เลขบัตรประชาชน 13 หลัก ระบบจะสร้าง QR Code จากเลขบัตรของคุณ</p>

            <form action="/register/external" method="post" id="externalForm" class="row g-3 needs-validation" novalidate>
              <div class="col-12">
                <label class="form-label req">อีเมล</label>
                <input type="email" name="email" class="form-control" required>
                <div class="invalid-feedback">กรุณากรอกอีเมลให้ถูกต้อง</div>
              </div>

              <div class="col-12">
                <label class="form-label req">รหัสบัตรประชาชน</label>
                <input type="text" name="citizen_id" class="form-control" maxlength="13" required>
                <div class="invalid-feedback">กรุณากรอกเลขบัตร 13 หลักเป็นตัวเลข</div>
              </div>

              <div class="col-12">
                <label class="form-label req">ชื่อ-นามสกุล</label>
                <input type="text" name="full_name" class="form-control" required>
                <div class="invalid-feedback">กรุณากรอกชื่อ-นามสกุล</div>
              </div>

              <div class="col-12">
                <label class="form-label req">เบอร์โทรศัพท์</label>
                <input type="tel" name="phone" class="form-control" required>
                <div class="invalid-feedback">กรุณากรอกเบอร์โทรศัพท์</div>
              </div>

              <div class="col-12 d-grid mt-2">
                <button type="submit" class="btn btn-success btn-lg">สมัครสมาชิก</button>
              </div>
            </form>
          </div>
      </div>

      <p class="text-center text-muted small mt-3 mb-0">มีปัญหาในการสมัคร? ติดต่อเจ้าหน้าที่ประจำศูนย์</p>
    </div>
  </div>

  <!-- Bootstrap bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Front-end validation -->
  <script>
    (function () {
      const form = document.getElementById('externalForm');
      form.addEventListener('submit', function (e) {
        const cid = form.citizen_id.value.trim();
        if (!/^[0-9]{13}$/.test(cid)) {
          e.preventDefault();
          e.stopPropagation();
          form.citizen_id.classList.add('is-invalid');
        }
        if (!form.checkValidity()) {
          e.preventDefault();
          e.stopPropagation();
        }
        form.classList.add('was-validated');
      });

      ['email','citizen_id','full_name','phone'].forEach(n=>{
        const el = form[n];
        if(el){
          el.addEventListener('input', ()=> el.classList.remove('is-invalid'));
        }
      });
    })();
  </script>
</body>
</html>
